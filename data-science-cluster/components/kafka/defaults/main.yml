---
kafka_home_dir: /usr/hdp/current/kafka-broker
kafka_log_dir: /var/log/kafka
kafka_conf_dir: /etc/kafka/conf
kafka_user: kafka
kafka_group: kafka
kafka_port: 9092
# Note, this is not the log4j like logs, but the topic logs:
kafka_logs: /tmp/kafka-logs
kafka_pid_dir: /var/run/kafka

#!/bin/sh

export SPARK_HOME="{{ spark_client_home }}"
export PYSPARK_PYTHON="{{ anaconda_install_dir }}/envs/{{ conda_env_name }}/bin/python"
export PYTHONPATH="{{ spark_client_home }}/python/:{{ spark_client_py4j_path }}"
export PYTHONSTARTUP="{{ spark_client_home }}/python/pyspark/shell.py"
export PYSPARK_SUBMIT_ARGS="--master local[*] pyspark-shell --packages com.databricks:spark-csv_2.11:{{ spark_client_version.split('-')[0] }}"
